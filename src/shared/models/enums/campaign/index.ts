export enum CampaignStatus {
  ACTIVE = 'ACTIVE',
  PAUSED = 'PAUSED',
  ARCHIVED = 'ARCHIVED',
}

/** Como llego el cliente al formulario. */
export enum FeedbackSource {
  QR = 'QR',
  LINK = 'LINK',
}
